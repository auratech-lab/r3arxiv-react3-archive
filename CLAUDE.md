@AGENTS.md

# Claude Code Notes

Use `docs/agent/README.md` and `llms.txt` as the first agent-facing entry
points for the R3ARXIV collection. The generated catalog and JSONL files are
more reliable than scraping the React UI.
